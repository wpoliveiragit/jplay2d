package projetos.pacman;

/**
 * Definicao da atalizacao da sprite.
 * 
 * @author Cantina
 *
 */
public interface AtualizarMenuDesenho {

	/**
	 * Define como a atualizacao da sprite deve reagir ao escolher uma determinada
	 * posicao.
	 * 
	 * @param index a posicao atual da selecao.
	 */
	public void atualizaMenuDesenho(int index);
}
